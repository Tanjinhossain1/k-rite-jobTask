import Image from "next/image";
import styles from "./page.module.css";

export default function Home() {
  return (
    <div>
      {/* here don't have any content because all are in layout  */}
    </div>
  );
}
